# Build dependencies for build.py
# Install with: pip install -r requirements.txt
Jinja2==3.1.6
PyYAML==6.0.3
weasyprint==68.0

# Only needed by make_og_image.py, which rasterises the social preview image
pymupdf==1.26.7
